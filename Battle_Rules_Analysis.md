# 전투 규칙 분석 (ArenaBattle — 현재 코드 기준)

> "실행은 되는데 룰이 지저분해서 뭘 하는지 모르겠다" 에 대한 답.
> **코드가 실제로 하는 것**을 게임 규칙 언어로 정리했다. (분석일: 2026-07-27, `ArenaBattle/Assets/Scripts` 기준)

---

## 0. 한 줄 요약

현재 이 전투는 **"한 판짜리(단일 전투), 속도순 턴제 배틀"** 이다.
- 아군/적을 **속도(spd) 내림차순**으로 한 번 정렬해 순서대로 행동한다.
- 한 캐릭터가 행동 = 스킬 1개를 대상에게 사용 → **데미지 = `max(1, 공격자.atk − 대상.def)`**.
- HP 0 → 사망(리스트·턴에서 제거·숨김). **한쪽이 전멸하면 전투 종료(WIN/LOSE 로그).**

그리고 **겉보기엔 화려하지만 실제론 안 도는 것**이 많다: **웨이브, 속성 상성, 버스트, 힐, 멀티히트 데미지, 스킬 계수, 스택, 매혹(Fascination)** 은 전부 장식/스텁이다. "지저분해 보이는" 이유가 이것 — 테이블·UI·enum은 풀 기능처럼 있는데 **실제 전투 계산은 서버가 하던 걸 임시로 `atk−def` 하나로 때운 상태**다.

---

## 1. 전투 루프 (턴/라운드/웨이브)

FSM: `Load → Intro → (턴루프) → GameResult`
턴루프: **`InitTurn → SkillSelect → CharacterAction → SetRoundUI → InitData → TurnEnd → (전멸체크) → InitTurn`**

| 개념 | 정의 | 실제 동작 |
|---|---|---|
| **턴(Turn)** | 한 캐릭터가 1회 행동 | `CurrentTurn` 단조 증가. 현재 행동자 = `roundDic[CurrentRound][(CurrentTurn-1)%10]` |
| **라운드(Round)** | 턴 10개 묶음 | `CurrentRound = (CurrentTurn-1)/10 + 1`. 라운드 넘어갈 때 옛 라운드 폐기 + `ChangeAttribute()` |
| **웨이브(Wave)** | 원래 "적 그룹 여러 번" 의도 | ❌ **미구현.** `CurrentWave`는 **항상 1**, `MaxWave`는 **설정·사용 안 됨** → 사실상 단일 전투 |

- 전투 종료는 **웨이브가 아니라 전멸**로만: `TurnEnd`의 `CheckBattleEnd` — `EnemyList.Count==0`(승) 또는 `AllyList.Count==0`(패) → `GameResult`. (양쪽 동시 전멸은 WIN 처리 — 버그성 엣지케이스)

---

## 2. 턴 순서 (속도순, 1회 고정)

- 전투 시작 시 `TurnManager.InitRound()`가 **아군+적 전체를 하나로 합쳐 `spd`(속도) 내림차순 정렬** → `roundDic`(라운드별 10칸 배열)에 순서대로 채움.
- **정렬은 딱 한 번.** 라운드마다 재정렬하지 않는다. 새 라운드는 같은 순서로 확장.
- 스킬의 `turn_next`/`turn_control`로 특정 캐릭터를 뒤 순번에 끼워넣을 수 있음(턴 조작).
- ⚠️ 순서 결정 스탯은 **`agi`가 아니라 `spd`**. (agi는 로드만 되고 안 씀)

---

## 3. 스킬 선택 (플레이어 vs AI)

| 행동자 | 스킬 선택 방식 |
|---|---|
| **아군 + CC 없음(`Auto_AI==None`)** | **플레이어 입력 대기** (그리드/버튼 클릭) |
| **적 전체** | **AI 자동** — 항상 **첫 번째 사용 가능 스킬(슬롯0)** = 결정적(랜덤 아님) |
| **CC 걸린 아군** | **AI 강제** (아래 CC 참고) |

AI 스킬 픽 규칙(`AIManager.MakeSkill`):
- **Anger(분노)**: 무조건 **기본공격(skillData[0])만**. 기본공격이 잠겨있으면 그 턴은 **아무것도 안 함**.
- **그 외**: `AttackLock`으로 막히지 않은 슬롯 중 → **적은 슬롯0 고정**, **AI 아군은 랜덤**.
- `Attack_Lock`(Basic/Skill1/Skill2 비트플래그) = **어떤 스킬 슬롯이 금지됐는지** 표시(스턴 아님, 후보에서 제외만).

---

## 4. 타겟팅

| 행동자 | 타겟 규칙 |
|---|---|
| **플레이어** | 스킬의 `type_radius`(그리드 AoE 패턴)에 걸린 셀 = 대상 → **멀티타겟 가능**. `TargetLock` 대상은 제외 |
| **AI** | **1명 랜덤**. 보통 **상대 진영만**, 단 **Confusion(혼란)이면 아군+적 양쪽**에서 랜덤 |

---

## 5. CC(상태이상) 시스템

CC는 스킬의 `trigger_id` → `Trigger.csv`/`Skill_Cc.csv`로 대상에게 부착(각자 `duration_turn` 보유). 매 `InitTurn`에 캐릭터의 활성 CC들을 접어(fold) `CharacterState`로 재계산, 매 `TurnEnd`에 지속시간 감소.

| CC | 효과 | 상태 |
|---|---|---|
| **ActionLock (Impossible)** | **스턴** — 그 턴 자동 패스 | ✅ 작동 |
| **TargetLock (Impossible)** | **타겟 불가** — 플레이어/AI 타겟풀에서 제외 | ✅ 작동 |
| **Anger(분노)** | 강제 AI + 기본공격만 | ✅ 작동 |
| **Confusion(혼란)** | 강제 AI + **아군/적 아무나** 랜덤 공격 | ✅ 작동 |
| **Fascination(매혹)** | (의도: 조종 상실) | ❌ **코드 분기 없음 = 일반 AI처럼 행동. 미구현** |
| **CC_Clear (Possible)** | 피격 시 해당 CC 해제("맞으면 풀림") | ✅ 작동 |
| **AttackLock (Basic/Skill1/2/Skills/All)** | 특정 스킬 슬롯 봉인 (`Skills`=기본 제외 전부, `All`=전부) | ✅ 작동 |

---

## 6. 데미지 & 스탯 (⚠️ 여기가 핵심)

**로드되는 스탯**: `str, vit, agi, dex, luk, atk, def, def_pen, hp, hit, avd, cri, cri_dmg, res, spd` (CSV에서 캐릭터별 로드)

**실제 전투에 쓰이는 스탯**: **`atk`, `def`, `hp`, `spd`(순서용)** — **이 4개뿐.**
나머지(str/vit/agi/dex/luk/def_pen/hit/avd/cri/cri_dmg/res)는 **전부 로드만 되고 안 씀.**

**데미지 공식(현행 임시)**:
```
dmg = max(1, 공격자.atk - 대상.def)
대상.hp -= dmg
```
- 대상당 **1회만** 적용. 크리티컬·속성·def_pen·스킬 계수(dmg_rate)·멀티히트 **전부 무시**.
- `multi_hit_count`는 **연출(히트 애니)만** 반복, 데미지는 그대로 1회. → **5히트 스킬도 데미지는 한 번.**
- 화면의 피격 연출과 실제 hp 감소는 **따로 논다**(hp는 이미 깎이고 애니는 장식).
- 코드 주석이 명시: 이건 **서버가 하던 계산을 오프라인용으로 임시 대체**한 것.

**HP/사망/힐**:
- HP는 **현재/최대 구분 없이 단일 `hp` 필드**를 직접 깎음.
- `hp<=0` → `KillCharacter`: 리스트·`roundDic`에서 제거 + 오브젝트 숨김. (사망 애니 `Death()`는 빈 스텁)
- **힐 완전 미구현.** `OnHeal()` 빈 스텁. 어디서도 `hp +=` 없음. → **`type_calculation=Heal`인 스킬도 그냥 `atk−def` 데미지를 줌.**

---

## 7. 버스트 게이지 (스텁)

- **팀 공유 게이지 1개**(`InGameData.BurstGauge`, float). 스킬 쓸 때마다 `burst_point`만큼 충전. 임계값 100.
- ❌ **페이백 없음.** 게이지가 꽉 차도 **아무 일도 안 일어남**: UI는 슬라이더/`%`만 갱신, 발동 블록은 비어있음. 소모/리셋/버스트공격 전부 없음.
- 게다가 `result >= 100` 체크가 `result`(0~1 값)와 100을 비교하는 **로직 버그**라 그 분기는 애초에 안 탐. `BurstMode` enum·`BurstTable`도 정의만 되고 미사용.

---

## 8. 속성(Attribute) — 장식일 뿐

- enum `Attributes { Day=1, Night, Dawn, Eclipse }`. `Eclipse`는 경계용 센티넬.
- `ChangeAttribute()`: **라운드마다 Day→Night→Dawn→Day…** 순환.
- ❌ **전투 효과 0.** 데미지 공식에 속성항 없음. "낮 속성 디노가 강해진다" 같은 상성 버프 로직(`TimeSystem`, `CalculateSystem`)은 **전부 주석 처리(dead).**
- 스테이지의 `time_start`(시작 속성)도 **파싱만 하고 적용 안 함** → 항상 사실상 Day부터 시작.
- 속성 아이콘은 **순수 UI 표시**용.

---

## 9. "있어 보이지만 실제론 안 도는 것" 목록 (지저분함의 정체)

| 시스템 | 상태 |
|---|---|
| **웨이브** | ❌ 미구현 (항상 1웨이브, 단일 전투) |
| **속성 상성/버프** | ❌ 장식 (순환·아이콘만, 전투효과 0, 실제 버프코드 주석처리) |
| **버스트 발동** | ❌ 스텁 (충전만, 페이백/소모 없음, 비교 버그) |
| **힐** | ❌ 스텁 (Heal 스킬도 데미지를 줌) |
| **멀티히트 데미지** | ❌ 연출만 반복, 데미지 1회 |
| **스킬 계수(dmg_rate/보조스탯 스케일)** | ❌ 무시, `atk−def`만 |
| **스택(Stack)** | ❌ 트리거 Stack 분기 비어있음 |
| **Fascination(매혹)** | ❌ 분기 없음 (일반 AI로 동작) |
| **대부분의 스탯** | ❌ 로드만, 미사용 (str/vit/agi/dex/luk/def_pen/hit/avd/cri/cri_dmg/res) |
| **결과 화면/보상** | ❌ WIN/LOSE 로그만, UI·보상 없음 |
| **죽은 중복 enum** | `[Flags] Auto_AI` vs `Type_Target_Auto_AI`, `Attributes` vs `CharacterAttribute` — 각각 하나는 dead |

---

## 10. 그래서 "실제 게임"은 이렇게 흐른다 (한 판 walkthrough)

1. 스테이지 진입 → 아군(오프라인 테스트 스쿼드) + 적(스테이지 몬스터) 스폰.
2. 전원을 **spd 내림차순**으로 정렬해 행동 순서 확정.
3. 순서대로:
   - **내 아군(CC 없음)** → 내가 스킬+타겟 선택.
   - **적/CC아군** → AI가 (적은) 첫 스킬을, 랜덤/강제 타겟에 사용.
   - 대상에게 **`atk−def` 데미지** → hp 0이면 사망(사라짐). 스킬 트리거로 CC/버프 부착.
4. 10턴마다 라운드 넘어가며 속성 아이콘만 바뀜(효과 없음).
5. **적이 다 죽으면 WIN, 아군이 다 죽으면 LOSE** 로그 후 정지.

즉 **핵심 루프(순서→행동→데미지→사망→전멸판정)만 살아있고**, 그 위에 얹힌 RPG 시스템(웨이브/속성/버스트/힐/스킬계수)은 **테이블·UI는 있으나 계산이 비어있는 상태**다. 원래 **서버가 이 계산들을 다 했기 때문**이고, 오프라인 전환하며 `atk−def` 하나로만 임시 복원했기 때문이다.

---

## 참고: 대개편 시 우선순위 제안 (선택)

"게임답게" 만들려면 빈 계산부터 채우는 순서:
1. **데미지 공식** — 스킬 `dmg_rate` + 보조스탯 스케일 + 크리(cri/cri_dmg) 반영 (지금 `atk−def`).
2. **웨이브 루프** — `MaxWave`/다음 웨이브 전환 구현 (지금 단일 전투).
3. **속성 상성** — `TimeSystem` 되살려 데미지 배율 (지금 장식).
4. **버스트 페이백** — 100 도달 시 발동/소모.
5. **힐** — `type_calculation=Heal` 처리.
6. **결과 화면/보상** — GameResult → Result UI 연동.
